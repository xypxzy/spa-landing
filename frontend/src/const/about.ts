export interface CarouselProps{
    image: string;
    name: string;
    description: string;
}
export interface TeamProps{
    first_name: string;
    last_name: string;
    position: string;
}
export interface AdrressProps{
    
}
